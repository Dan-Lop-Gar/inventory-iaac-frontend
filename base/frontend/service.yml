apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: inventory-mgmt
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30200
  type: NodePort